export enum EMethodRequest {
  GET = "GET",
  POST = "POST",
  DELETE = "DELETE"
}

export enum ETypeLogin {
  LOGIN = "login",
  AUTH_TOKEN = "auth"
}

export enum ETypeActionFiles {
  GET_FILES = "files"
}
